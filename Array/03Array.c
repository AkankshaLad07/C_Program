/// Program to print value of array element without size of array,
/// but with initializing of array element

    #include<stdio.h>
    #include<conio.h>
    int main()
    {
        int Values[] = {12, 43, 57, 86, 90};

        printf("\nValue of 1st element => %d.",Values[0]);
        printf("\nValue of 2nd element => %d.",Values[1]);
        printf("\nValue of 3rd element => %d.",Values[2]);
        printf("\nValue of 4th element => %d.",Values[3]);
        printf("\nValue of 5th element => %d.",Values[4]);

        getch();
        return 0;
    }
