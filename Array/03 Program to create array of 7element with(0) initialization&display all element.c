///Program to create array of 7element with(0) initialization & display all element.

    #include<stdio.h>
    #include<conio.h>

    int main()
    {
        int Arr[5]={};

        printf("\nValue of element 1 is => %d.",Arr[0]);
        printf("\nValue of element 2 is => %d.",Arr[1]);
        printf("\nValue of element 3 is => %d.",Arr[2]);
        printf("\nValue of element 4 is => %d.",Arr[3]);
        printf("\nValue of element 5 is => %d.",Arr[4]);

        getch();
        return 0;
    }
